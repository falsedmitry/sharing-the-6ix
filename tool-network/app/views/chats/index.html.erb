<h1>Messages</h1>
<% @chats.each do |chat| %>
  <div class=“chats”>
    <% chat.tool.owner_pictures.each_with_index do |picture, index| %>
      <img src="<%= chat.tool.owner_pictures[index] %>" alt="", class="avatar">
    <% end %>
    <p><%= link_to(chat.tool.name, edit_chat_path(chat)) %></p>
    <p>From: <%= chat.user.name %></p>
    <hr>
  </div>
<% end %>
